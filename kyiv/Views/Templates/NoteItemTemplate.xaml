<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kyiv.Views.Templates.NoteItemTemplate"
             xmlns:model="clr-namespace:kyiv.Models"
             x:DataType="model:NoteModel">
    <ContentView.Resources>
        <ResourceDictionary>
            <Color x:Key="DarkBackgroundMarkColor">#CADCFC</Color>
            <Color x:Key="WhiteBackgroundMarkColor">#1995AD</Color>

        </ResourceDictionary>
    </ContentView.Resources>
    <Grid ColumnDefinitions="*" 
      RowDefinitions="Auto, Auto"
      HorizontalOptions="Fill" 
      VerticalOptions="Start">

        
        <!-- Border займає всю ширину екрана -->
        <Border Margin="10"

            HorizontalOptions="Fill" 
            VerticalOptions="Start"
            Padding="10"
            BackgroundColor="White"
            HeightRequest="250"
            StrokeShape="RoundRectangle 10"
            StrokeThickness="1"
            Grid.Row="0">

            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnNoteTapped"/>
            </Border.GestureRecognizers>

            <!-- Внутрішній Grid для вмісту -->
            <Grid RowDefinitions="Auto" 
              RowSpacing="5" 
              ColumnDefinitions="2*,*"
              HorizontalOptions="Fill">

                <Label Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"
                   Text="{Binding Text}" 
                   LineBreakMode="WordWrap"
                   MaxLines="5"
                   Margin="0,5,0,5"
                       TextColor="Black"
                   HorizontalOptions="Start"
                   VerticalOptions="Fill">
                </Label>

            </Grid>
        </Border>
        <Label Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"
                   Text="{Binding Name}" 
                   FontSize="18"
                   LineBreakMode="TailTruncation"
                   MaxLines="2"
                   HorizontalOptions="Center"
               Margin="0, 0, 0, 30">
        </Label>
    </Grid>
</ContentView>
